# Cypress TS Project

Sample Cypress project (TypeScript) for AAA_playwright-projects.

Usage:

```bash
cd AAA_playwright-projects/cypress-ts
npm install
npm run build   # optional, compiles TS support files
npm run cy:open
# or
npm run cy:run
```
